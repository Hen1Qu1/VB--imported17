
function control(){ 

    var valor = window.document.getElementById('vb-quantid');
    var value = Number(valor.value);

    if(value < 1  || value > 100){
        window.alert('[ERRO] - INFORME UM NÚMERO DE 1 A 100.');
    }
}



function control2(){ 

    var valor = window.document.getElementById('vb-quantid2');
    var value = Number(valor.value);

    if(value < 1  || value > 100){
        window.alert('[ERRO] - INFORME UM NÚMERO DE 1 A 100.');
    }
}

function control3(){ 

    var valor = window.document.getElementById('vb-quantid3');
    var value = Number(valor.value);

    if(value < 1  || value > 100){
        window.alert('[ERRO] - INFORME UM NÚMERO DE 1 A 100.');
    }
}

function control4(){ 

    var valor = window.document.getElementById('vb-quantid4');
    var value = Number(valor.value);

    if(value < 1  || value > 100){
        window.alert('[ERRO] - INFORME UM NÚMERO DE 1 A 100.');
    }
}

function control5(){ 

    var valor = window.document.getElementById('vb-quantid5');
    var value = Number(valor.value);

    if(value < 1  || value > 100){
        window.alert('[ERRO] - INFORME UM NÚMERO DE 1 A 100.');
    }
}

